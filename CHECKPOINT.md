# CHECKPOINT — Public Demo

> آخر تحديث: 2026-08-20 15:27 (+03:00)
> هذا الملف هو نقطة الاستئناف العملية لنسخة العرض التجريبية العامة. اقرأه قبل استكمال أي عمل على العرض.

## النطاق الحالي

- المستودع: `asm114/adwaa-alsharq`
- فرع العمل الأساسي للعرض: `demo/public-trial`
- لا يتم تعديل `main` ضمن عمل نسخة العرض.
- لا يتم دمج نسخة العرض في `main` أو `commercial-template` بدون موافقة صريحة منفصلة.
- مشروع Vercel الخاص بالعرض: `adwaa-demo-public-v4`
- العرض ما زال **معاينة محمية** ولم يُعتمد للنشر العام النهائي.

## نقاط الأمان

- فرع الأمان الأول: `backup-demo-public-trial-20260820-1506` عند Commit `93b8a9486435740c3b13fc8a9f24cd87c61f48a3`.
- قبل إصلاح تعليق شاشة البداية تم أخذ نسخة أمان إضافية: `backup-demo-before-loading-fix-20260820-1518` عند Commit `c830a27c73f7d54cd099a8f5dddbaea69c4972f1`.
- لا تحذف أو تعيد كتابة فروع الأمان بدون موافقة صريحة.
- لا تستخدم Force Push على هذه الفروع.

## آخر إصلاح — تعليق شاشة البداية والاسم

أظهر اختبار المستخدم على سطح المكتب أن شاشة البداية بقيت معلقة، وكان اسم الإدارة `منتجع العرض التجريبي` غير مناسب.

تم تحديد سبب محتمل وقابل للإزالة: بناء العرض كان ما يزال يحتوي على تحميل مكتبة Supabase من CDN خارجي قبل تشغيل المحاكاة المحلية. هذا الاعتماد الخارجي غير مطلوب أصلًا لنسخة العرض المحلية، ويمكن أن يمنع اكتمال تحميل الصفحة إذا تعذر الوصول إليه.

الإصلاح معزول في فرع `demo/public-trial` فقط:

- أضيف `demo-supabase-shim.js` كبداية محلية بسيطة تسمح لـ `demo-local-runtime.js` باستبدال `createClient` بالمحاكاة المحلية.
- أضيف `tools/build-demo-deployment-v2.mjs`، ويشغل البناء السابق ثم يزيل اعتماد Supabase CDN الخارجي من ملفات HTML الناتجة ويستبدله بالـshim المحلي.
- عُدل `vercel.json` ليشغّل `node tools/build-demo-deployment-v2.mjs`.
- اسم شاشة الإدارة في ناتج العرض صار `نظام إدارة الحجوزات`، والوصف `نسخة تجريبية للعرض` بدل الاسم القديم الغريب.
- إعداد العرض يستخدم `نظام إدارة الحجوزات — نسخة تجريبية` كاسم عرض عام.
- لا يلزم تغيير اسم المنشأة الوهمية داخل بوابة العميل إذا احتاجت البوابة اسم عقار تجريبي للسياق؛ المهم ألا يظهر اسم أضواء الشرق الحقيقي.

الـdiff النهائي بين نقطة الأمان قبل الإصلاح والفرع الحالي يحتوي فقط على:

1. `demo-supabase-shim.js` — ملف جديد.
2. `tools/build-demo-deployment-v2.mjs` — ملف جديد.
3. `vercel.json` — تعديل أمر البناء فقط.

لا توجد ملفات مؤقتة أو تغييرات جانبية في الـdiff النهائي.

## تحقق Vercel للإصلاح

- Commit الكود الذي تم اختباره: `579c85714e8072c43b908840e4e0b3956cee5c7e`.
- Vercel بنى Preview من الفرع `demo/public-trial` نفسه، وليس من `main`.
- Deployment الذي اختُبر في مرحلة البناء: `dpl_H57RfFaJMXNLVw9NE8TDwfyfSEsS`.
- سجل البناء أكد تشغيل `node tools/build-demo-deployment-v2.mjs`.
- سجل البناء أكد نجاح البناء وإزالة اعتماد Supabase CDN الخارجي واستخدام الاسم الموحّد للعرض.
- فحص Runtime للـdeployment لم يُظهر سجلات warning/error/fatal ضمن نافذة الفحص.
- حماية Vercel ما زالت مفعلة؛ لم يتم فتح المشروع للعامة.
- التحقق البصري في متصفح المستخدم بعد هذا الإصلاح **ما زال مطلوبًا** قبل اعتبار مشكلة التعليق مغلقة نهائيًا.
- روابط `_vercel_share` مؤقتة ولا تُحفظ هنا لأنها تنتهي صلاحيتها.

## قرارات نسخة العرض المعتمدة

1. نسخة العرض تستخدم **آخر واجهة مطورة**، وليس الشكل القديم.
2. يمنع ظهور بيانات أضواء الشرق الحقيقية أو بيانات المستخدم الحقيقية في العرض.
3. النظام الإداري في العرض يعمل ببيانات وهمية ومحلية لكل زائر؛ لا يكتب على قاعدة الإدارة الحقيقية ولا على قاعدة مشتركة بين الزوار.
4. بوابة العميل في العرض تستخدم بيانات تجريبية فقط وتعمل في وضع محلي/معزول.
5. لا يتم إعطاء الزوار حساب المدير التجريبي المشترك كآلية العرض العامة.
6. لا توجد مفاتيح Service Role أو أسرار تشغيلية داخل نسخة العرض أو GitHub.
7. التواصل الحقيقي مثل واتساب/الروابط الخارجية وطلبات الحجز الحقيقية يجب أن يبقى معطلاً أو محاكًى داخل العرض.
8. يجب إبقاء عبارة واضحة بأن النسخة تجريبية وأن البيانات غير حقيقية.
9. حقوق الملكية تبقى باسم **عبدالعزيز الفوزان** مع إشعار الحقوق المعتمد، دون أي ادعاء بترخيص أو اعتماد حكومي.
10. اسم الإدارة العام المعتمد للعرض: **نظام إدارة الحجوزات — نسخة تجريبية**.
11. لا يعتبر العرض جاهزًا للنشر العام لمجرد نجاح Build؛ يلزم اختبار بصري وسلوكي فعلي قبل الاعتماد.

## ما تم إصلاحه في مسار العرض

- تم التخلي عن محملات/أغلفة النشر القديمة التي سببت ظهور كود كنص أو `Failed to fetch`.
- تم الانتقال إلى بناء ملفات العرض الفعلية من فرع GitHub داخل Vercel.
- تم تنظيف القيم الافتراضية التي كانت قد تظهر معلومات أضواء الشرق في بوابة العميل.
- تم تجهيز إعداد Demo مستقل لا يعتمد على Supabase حي.
- تم تجهيز عزل محلي للبيانات حتى لا يغير زائر بيانات زائر آخر.
- تم إزالة تحميل Supabase CDN الخارجي من ناتج العرض الأخير لتقليل نقطة التعليق الخارجية.
- تم التأكد أن GitHub مربوط بمشروع Vercel `adwaa-demo-public-v4`، وأن عملية الربط نفسها لم تنشر `main` تلقائيًا وقت التحقق.

## ما لم يُعتمد بعد

- لم يتم اعتماد النسخة كإصدار عام نهائي.
- لم يتم إلغاء حماية Vercel للإنتاج.
- لم يتم Merge لفرع العرض إلى `main` أو `commercial-template`.
- لم يتم إثبات اختبار كامل على iPhone/iPad/Desktop لهذه المعاينة الأخيرة.
- يلزم إعادة فتح Preview الأخير في متصفح المستخدم للتحقق أن شاشة البداية لم تعد تعلق وأن الاسم الجديد ظاهر.
- لم يتم اعتماد بوابة العميل بصريًا من المستخدم بعد آخر Build.
- لا تُعتبر أي روابط v1/v2/v3 السابقة صالحة للاعتماد؛ كانت محاولات قديمة فيها مشاكل نشر.

## الخطوة التالية عند الاستئناف

1. تحقق من `demo/public-trial` ومن هذا الملف قبل أي تعديل.
2. حافظ على `backup-demo-before-loading-fix-20260820-1518` كنقطة رجوع مباشرة قبل إصلاح التعليق.
3. اختبر Preview مبنيًا من Commit الكود `579c85714e8072c43b908840e4e0b3956cee5c7e` أو ما بعده إذا كان التغيير توثيقًا فقط.
4. افتح Preview المحمي عبر رابط مشاركة مؤقت جديد عند الحاجة.
5. تحقق أولًا أن شاشة البداية تكمل الدخول وأن الاسم يظهر `نظام إدارة الحجوزات`.
6. بعدها اختبر بصريًا وسلوكيًا: الصفحة الرئيسية، الحجوزات، المالية، التنبيهات، الإعدادات، ثم بوابة العميل.
7. افحص أن البيانات وهمية، وأن اسم/رقم/موقع أضواء الشرق غير ظاهر، وأن التواصل الحقيقي معطل.
8. إذا نجحت الاختبارات، اتخذ قرارًا منفصلًا بشأن تحويل النسخة إلى Production عام وإزالة الحماية عن Production فقط.
9. لا تفتح النشر العام قبل موافقة المستخدم الصريحة بعد الاختبار.

## قاعدة الاسترجاع

إذا ضاع السياق أو ظهرت نتيجة غريبة:

- لا تحاول إصلاح `main`.
- ارجع أولًا إلى فرع `backup-demo-before-loading-fix-20260820-1518` أو Commit `c830a27c73f7d54cd099a8f5dddbaea69c4972f1` للمقارنة قبل إصلاح التعليق.
- للرجوع إلى الحالة الأقدم، استخدم `backup-demo-public-trial-20260820-1506` عند `93b8a9486435740c3b13fc8a9f24cd87c61f48a3`.
- افحص الـdiff قبل أي استعادة أو نقل commits.
- لا تستخدم Force Push ولا تحذف فروع الأمان.

## ملاحظة تشغيلية

`AGENTS.md` يظل مرجع قواعد الأمان والتنفيذ الدائمة، و`PROJECT_CONTEXT.md` مرجع سياق المشروع العام. هذا الملف `CHECKPOINT.md` مخصص فقط لآخر نقطة تشغيلية موثقة لنسخة العرض التجريبية.