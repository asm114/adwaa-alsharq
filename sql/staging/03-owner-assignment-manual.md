# Manual owner assignment

Copy `supabase-set-staging-owner.sql.example` to a private ignored file, replace
`<STAGING_MANAGER_USER_UUID>` with the verified staging Auth user UUID, and run
it manually. Never commit the resulting file or UUID.

