# خطة التحقق العملي — Supabase Staging

هذه الخطة مخصّصة لمشروع Supabase تجريبي مستقل. يمنع تشغيلها على المشروع
المستخدم في الإنتاج، كما يمنع استخدام بيانات عملاء حقيقية أو مفاتيح
`service_role` داخل المتصفح.

## متطلبات البدء

1. مشروع Supabase Staging مستقل، مع توثيق اسمه ومرجعه دون نشر المفاتيح.
2. نسخة احتياطية قابلة للاستعادة من Staging قبل أي SQL.
3. ثلاثة حسابات اختبار مصطنعة:
   - مدير مالك؛
   - مستخدم مصادق غير مالك؛
   - عامل تنظيف لا يحتاج حسابًا ويستخدم رابط المهمة فقط.
4. صف `app_state/main` ببيانات اختبار مصطنعة فقط.
5. حفظ مخرجات `supabase-staging-preflight.sql` قبل التغيير.
6. حفظ نسخة من تعريف الجدول والسياسات والدوال والمنح الحالية.

## ترتيب التطبيق

1. أضف عمود `owner_id` فقط إن لم يكن موجودًا.
2. عيّن `owner_id` لصف `main` من لوحة SQL باستخدام UUID حساب المدير
   التجريبي الذي تم التحقق منه من `auth.users`. لا يمر UUID من الواجهة أو RPC.
3. تحقق أن صف `main` واحد فقط وأن `owner_id` غير فارغ.
4. راجع كل سياسة حالية. لا تحذف سياسة مجهولة قبل توثيقها وتحديد أثرها.
5. طبّق النسخة المراجعة من `supabase-security-review.sql`.
6. أعد تشغيل preflight وتأكد من RLS والمنح و`search_path`.

## مصفوفة RLS

| الهوية | SELECT | INSERT | UPDATE | DELETE |
|---|---:|---:|---:|---:|
| anon دون جلسة | رفض | رفض | رفض | رفض |
| مستخدم مصادق غير مالك | لا صفوف | رفض | لا صفوف معدّلة | لا صفوف محذوفة |
| المدير المالك | صف `main` فقط | صف يملكه فقط | صف يملكه فقط | حسب السياسة المعتمدة |

يجب اختبار كل خانة بطلب REST فعلي ومراجعة status/body دون طباعة JWT أو
publishable key. نجاح طلب SELECT برمز 200 مع مصفوفة فارغة يعني أن RLS أخفى
الصف؛ ولا يعد دليلًا على منح القراءة.

## بيانات المهمة المصطنعة

- UUID صالح للمهمة.
- Token عشوائي من 32 حرفًا hex.
- `accessExpiresAt` صالح.
- حجز تجريبي واحد مرتبط ومهمتان تجريبيتان غير مرتبطتين ببيانات حقيقية.
- نسخ منفصلة لاختبار token منتهي وملغي.

## اختبارات RPC

### `cleaner_get_task`

1. token صالح: يعيد DTO المهمة المحدود فقط.
2. غير صالح، منتهي، ملغي، UUID خاطئ، token أطول من 32، ومحاولات حقن:
   يعيد `null` ولا يكشف سببًا تفصيليًا.
3. افحص مفاتيح الاستجابة آليًا. يمنع وجود:
   `token`, `bookingId`, `bookings`, `expenses`, `settings`, `auditLog`,
   `backupHistory`, `notifications`, أو الحالة الكاملة.
4. افحص عناصر `photos` و`issues` نفسها للتأكد من عدم مرور مفاتيح إضافية.

### `cleaner_update_task`

1. حدّث الحالة والصور والملاحظات المسموحة فقط.
2. أضف مفاتيح `booking`, `expenses`, `settings`, `owner_id`, `token` وتأكد
   من رفض الطلب كاملًا.
3. أرسل `photos` و`issues` بأنواع خاطئة، عناصر زائدة، نصوصًا طويلة، SVG،
   `data:text/html`، و`javascript:` وتأكد من الرفض.
4. token غير صالح/منتهي/ملغي يرفض دون تعديل `updated_at`.
5. قارن نسخة state قبل وبعد الطلب، مع تجاهل حقول المهمة والإشعار المقصودة،
   لإثبات عدم تعديل الحجوزات أو المصروفات أو الإعدادات عشوائيًا.

## اختبار التزامن

1. احفظ `revision` الحالي للمهمة.
2. أرسل طلبين في اللحظة نفسها بنفس `expectedRevision` لكن بتعديلين مختلفين.
3. يجب أن ينجح طلب واحد فقط، ويرفض الآخر كتعارض؛ ثم يعيد العميل تحميل المهمة.
4. أرسل طلبين لمهمتين مختلفتين. يجب أن تبقى نتيجتا المهمتين محفوظتين.
5. `FOR UPDATE` على صف JSON الواحد يحقق التسلسل، لكنه لا يمنع Lost Update
   وحده إذا لم يوجد تحقق revision/نسخة متوقعة. لا يُقبل الاختبار إذا نجح
   طلبان قديمان وكتب الثاني فوق الأول بصمت.

## اختبار `cleaner.html`

1. شغّل نسخة محلية موجهة إلى Staging فقط، ولا تنشرها.
2. افتح رابط token صالح وسجل Network response بعد حجب URL/token.
3. تحقق أن الاستجابة DTO محدود وليست `app_state`.
4. حدّث المهمة وأعد فتحها.
5. عدّل طلب RPC من أدوات المطور وأضف حقولًا غير مسموحة؛ يجب أن يرفض الخادم.
6. لا تستخدم `service_role` أو Database password في الصفحة.

## اختبار `index.html`

نفّذ على بيانات مصطنعة: تسجيل الدخول، القراءة، إضافة/تعديل حجز، إضافة مصروف،
إنشاء وإلغاء مهمة، ثم تسجيل الخروج. يجب ألا تظهر البيانات بعد الخروج. اختبر
قفل الخمول وقراءة بيانات قديمة عبر Validators. احذف fixture التجريبي في نهاية
الاختبار بعد حفظ دليل النتائج.

## النسخ الاحتياطي

اختبر نسخة سليمة، ملفًا أكبر من الحد، JSON عميقًا، مفاتيح
`__proto__`/`constructor`/`prototype`، إصدار schema مجهولًا، حقولًا إضافية،
SVG و`data:text/html` و`javascript:`، وكل payloads XSS. لا تنفذ استعادة
فعلية على الإنتاج؛ في Staging فقط تُقبل الاستعادة بعد نقطة رجوع ناجحة.

## أدلة القبول

- مخرجات preflight قبل/بعد.
- status code وقائمة مفاتيح JSON لكل طلب مع حجب tokens.
- مقارنة hashes/أعداد عناصر fixture قبل/بعد.
- لقطة Network من cleaner تبين مفاتيح DTO فقط.
- نتيجة اختبار التزامن، بما فيها الطلب المرفوض كتعارض.
- سجل نجاح الاختبارات المحلية واختبارات المتصفح.

## Rollback

1. أوقف الواجهة التجريبية.
2. استعد Snapshot قاعدة Staging المأخوذ قبل التطبيق، وهو المسار المفضل.
3. إن تعذرت الاستعادة الكاملة، نفّذ rollback SQL تمت مراجعته مسبقًا لإزالة
   RPC والسياسات الجديدة وإعادة السياسات الموثقة حرفيًا من preflight.
4. لا تحذف `owner_id` قبل التأكد من عدم اعتماد أي كود تجريبي عليه.
5. أعد اختبارات القراءة والكتابة القديمة على Staging فقط.

