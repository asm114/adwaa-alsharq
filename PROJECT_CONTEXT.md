# PROJECT_CONTEXT — أضواء الشرق

> هذا الملف هو المرجع المختصر للمشروع عند بدء أي جلسة جديدة مع ChatGPT/Codex أو عند الانتقال بين الأجهزة.
> إذا تعارضت الذاكرة أو المحادثة مع حالة GitHub، تُراجع حالة المستودع والـdiff أولًا قبل اتخاذ قرار.

## طريقة الاستئناف في أي جلسة جديدة

1. اقرأ هذا الملف أولًا.
2. تحقق من آخر commit على `main` والفرع الذي يجري العمل عليه.
3. اقرأ فقط الملفات والـdiff المرتبط بالمهمة الحالية؛ لا تعِد تحليل المستودع كاملًا بلا حاجة.
4. لا تنفذ تغييرًا معماريًا أو في البيانات اعتمادًا على الذاكرة وحدها.
5. إذا ظهرت فكرة جديدة تغيّر اتجاه المشروع، صنّفها أولًا: متوافقة مع الخطة الحالية أم تغيير اتجاه يحتاج قرارًا صريحًا.

## الحالة المستقرة الحالية

- المستودع: `asm114/adwaa-alsharq`.
- `main` هو مصدر النسخة الحالية المنشورة.
- نقطة الاستقرار المرجعية الحالية: commit `485f17dc028f326689f4ebed3e62d955f100a696`.
- الفرع `stable-v1` محفوظ كمرجع للنسخة المستقرة الحالية ولا يُستخدم للتطوير اليومي.
- الفرع `commercial-template` مخصص لتحويل النظام إلى قالب تجاري، ولا يُنشر كإنتاج قبل اكتمال العزل والاختبارات.
- آخر بوابة تجارية مكتملة: PR #71 مدمج في `commercial-template` عند commit `0fcf0f0bdcb7f5a5cc9796a4a318a437cfc2569b`.
- الاستضافة الرسمية الحالية عبر GitHub Pages. لا تُحوّل نسخة الإنتاج الرسمية إلى Vercel دون قرار صريح.
- لا يُستخدم الفرع القديم `deploy/sync-main-to-pages` للنشر.

## هدف المشروع الحالي

الحفاظ على نسخة أضواء الشرق الحالية مستقرة، وبناء مسار تجاري منفصل يمكن تركيبه لعميل جديد دون مشاركة بيانات أضواء الشرق أو حساباتها، ثم استخدام أول نسخة مستقلة كتجربة قبل التوسع في البيع.

## حدود أمان لا تُكسر

- لا تُنسخ بيانات الحجوزات أو العملاء أو المستخدمين من الإنتاج إلى أي نسخة عميل.
- لا تُجرى كتابات أو migrations على قاعدة بيانات الإنتاج أثناء تجهيز القالب التجاري إلا بطلب صريح ومراجعة مستقلة.
- لا تُخزن كلمات مرور أو مفاتيح سرية أو service-role keys أو بيانات عملاء حساسة داخل GitHub.
- لا يُعتمد على hostname وحده لتحديد بيئة Supabase؛ نسخ GitHub Pages قد تشترك في نفس hostname وتختلف فقط في مسار المستودع.
- البوابة العامة للعميل تبقى منفصلة Backend عن تطبيق الإدارة الأساسي لكل نسخة، ما لم يصدر قرار معماري صريح يغيّر ذلك.
- لا تُحوّل بوابة العميل إلى Backend الإنتاج الأساسي لأضواء الشرق.
- كل نسخة عميل مستقبلية يجب أن تبدأ بقاعدة بيانات نظيفة وحساب مدير وصلاحيات RLS خاصة بها.
- يجب عزل مفاتيح التخزين المحلي وAuth وService Worker/Cache لكل نسخة حتى لا تتداخل النسخ مع بعضها.
- لا يُعاد تنظيم أو حذف مشاريع Supabase الحالية لمجرد تقليل عدد المشاريع أو تبسيط الحسابات ما لم توجد خطة ترحيل واختبار واسترجاع واضحة.
- التوقيع والختم الحقيقيان والعقود الموقعة وبيانات العميل الحساسة لا تُخزن في هذا المستودع العام؛ تُحفظ في Secure Storage — تخزين آمن خاص بالعميل أو في مسار نشر خاص به.

## ما يجب تحويله إلى إعدادات في القالب التجاري

- اسم المنشأة ونوعها وهوية العلامة.
- الشعار والنصوص التعريفية.
- قالب العقد ونصوصه، وهوية رأس/تذييل المستندات، ومراجع الشعار والتوقيع والختم الخاصة بالعميل.
- رقم واتساب وروابط التواصل والموقع والخريطة.
- أوقات الدخول والخروج والمزايا.
- إعدادات النشر ومسار الموقع.
- إعدادات Backend الأساسية وإعدادات بوابة العميل.
- حسابات المدير والسياسات الخاصة بكل عميل.

## سلوكيات تشغيلية معتمدة حاليًا

- تنبيه العقد لا يظهر في جرس التنبيهات؛ إنشاء/مشاركة العقد تتم من مسار الإرسال الخاص بالحجز.
- رسالة الترحيب/تأكيد الحجز يدوية عبر واتساب وليست تذكيرًا تلقائيًا مستقلًا.
- الفاتورة مخفية من التدفق اليومي ولا تُعاد كإجراء روتيني إلا بقرار جديد.
- لا تُعاد وظائف أو مسميات التنظيف القديمة أو "بوابة جميل".
- تشييك العامل مسار مستقل مرتبط بالحجز، مع اختيارات مرئية وصور مطلوبة وصوت اختياري عند الحاجة.
- أي تغيير في سياسات التذكير أو الإرسال يُوثق هنا إذا أصبح قرارًا دائمًا.

## نموذج البيع المعتمد مبدئيًا

- يُعامل الصديق كأول **عميل تجريبي حقيقي** للنظام، وليس كنسخة شخصية عشوائية.
- `commercial-template` هو **المصنع/المصدر الأساسي للكود والتحديثات**.
- GitHub يحفظ الكود، الإصدارات، migrations، التوثيق وآلية النشر؛ ولا يحفظ بيانات العملاء التشغيلية.
- العميل لا يحتاج عادةً إلى GitHub ولا يُسلّم له المستودع الأصلي أو السورس ما لم ينص اتفاق البيع صراحةً على نقل ملكية الكود.
- كل عميل يحصل على نسخة نشر مستقلة ومعزولة، وإعداداته وبياناته لا تختلط بعميل آخر.
- الأصل أن يكون Supabase وبيانات العميل تحت ملكية العميل أو منشأته عند البيع النهائي؛ ويمكن لاحقًا تقديم خيار "خدمة مُدارة" برسوم ودعم ومسؤوليات واضحة.
- الاستضافة/الدومين يمكن أن يكونا باسم العميل أو ضمن خدمة مُدارة، لكن يجب توثيق من يملك ماذا قبل التسليم.
- التعديلات العامة تُنفذ أولًا في `commercial-template` ثم تُمرر للنسخ العميلية بطريقة منظمة، وليس بتعديلات يدوية مستقلة غير موثقة.

## سياسة تحديث قاعدة البيانات للعملاء

- أي تغيير Schema دائم في Supabase يجب أن يكون Migration محفوظًا في GitHub ومُرقّمًا/قابلًا للتتبع.
- لصق SQL يدويًا في Dashboard لكل عميل **ليس آلية تشغيل مقبولة للمنتج التجاري**.
- العميل الجديد يطبّق سلسلة migrations من البداية على قاعدة نظيفة.
- العميل الحالي يطبّق فقط migrations غير المطبقة لديه عند الترقية.
- أي إصلاح طارئ يدوي يجب أن يُعاد توثيقه فورًا كـ migration حتى لا ينشأ drift بين العملاء والكود.
- الهدف النهائي هو أن يكون نشر الإصدار قادرًا على تحديث كود الواجهة وقاعدة البيانات للعميل المستهدف بصورة منظمة وقابلة للتراجع.
- في البداية تُدار التحديثات من GitHub/أداة نشر خاصة بالمطور، وليس بزر تحديث داخل واجهة العميل.
- ترتيب الإطلاق المفضل: اختبار معزول → أول عميل تجريبي → ثم بقية العملاء بعد التحقق.

## قرار Supabase الحالي

- نسخة أضواء الشرق الحالية مستقرة على فصل Backend الإدارة عن Backend بوابة العميل؛ لا يتم دمجهما الآن لمجرد توفير خانة مجانية.
- يمكن تقييم تصميم تجاري أبسط (مثل Backend واحد آمن للإدارة والبوابة) **فقط داخل نسخة تجريبية معزولة**، ثم اتخاذ قرار بناءً على الاختبارات.
- ملكية مشاريع Supabase الحالية موزعة على أكثر من حساب يملكه المستخدم. خريطة الإيميلات/الملكية الدقيقة لا تُكتب في هذا المستودع العام؛ تحفظ خارجه ولا تُستخدم ذريعة لتغييرات إنتاجية سريعة.

## آخر إنجازات التحويل التجاري

- اكتمل **Commercial Config — إعداد النسخة التجارية** بنظام Fail-closed؛ لا اتصال إذا بقيت قيم `CHANGE_ME_*`.
- أزيل اعتماد القالب التجاري على hostname لاختيار Supabase.
- بوابة العميل العامة وإدارة البوابة تقرآن Backend العميل الصريح بدل مراجع أضواء الشرق الثابتة.
- اكتملت **Branding Layer — طبقة الهوية التجارية**: الاسم، نوع المنشأة، الموقع والوصف من مصدر إعداد واحد، مع Manifest تجاري ديناميكي.
- عُزلت مفاتيح Local/Session Storage القديمة تحت Namespace خاص بكل نسخة.
- عُزل Cache الخاص بـService Worker حسب مسار النشر، ولا تحذف النسخة كاش نسخة أخرى على نفس النطاق.
- تشييك العامل لم يعد يحمل Project Ref/Publishable Key ثابتين لأضواء الشرق؛ يقرأ Backend بوابة العميل ويفشل مغلقًا إذا لم تكتمل الإعدادات.
- تأسس مسار **Commercial Migrations — الترحيلات التجارية** الجديد تحت `supabase/commercial`، منفصلًا عن تاريخ Staging القديم.
- فُصلت ترحيلات **Core — الإدارة** عن ترحيلات **Portal — بوابة العملاء** لكل نسخة تجارية.
- Core التجاري أصبح ينشئ `app_state` ويحدد المدير بعضوية UUID عبر `auth.uid()` بدل بريد مطور ثابت.
- Portal التجاري أصبح يعرّف `is_resort_admin()` من جدول عضوية خاص بالعميل، بدون بريد مدير ثابت داخل Migration.
- Migration معلومات المنشأة التجارية أصبحت Schema فقط بدون Seed أضواء الشرق أو روابطه، واستقبال طلبات الحجز يبقى مغلقًا افتراضيًا حتى تهيئة بيانات العميل.
- ملفات Staging القديمة التي تحمل بريد/هوية أضواء الشرق بقيت للتاريخ والنسخة الحالية، لكنها مستبعدة صراحة من **Commercial Install — تركيب العميل التجاري**.
- اختبارات CI نجحت قبل دمج PR #71 في `commercial-template`.
- لم تتغير `main` تشغيلًا، ولم تُجرَ أي كتابة على Supabase أو Auth أو بيانات الإنتاج ضمن هذه المراحل.

## خطة التحويل التجاري

1. ✅ جرد ما هو خاص بأضواء الشرق أو حساباتها داخل `commercial-template`.
2. ✅ فصل إعدادات المنشأة والبيئة وSupabase عن منطق التطبيق الأساسي.
3. ✅ إزالة الاعتماد على hostname وإضافة Branding/Storage/Cache isolation الأساسية.
4. 🟡 **Commercial Migrations — ترحيلات قاعدة البيانات التجارية**: اكتمل الأساس لـCore وعضوية مدير Portal ومعلومات المنشأة؛ الخطوة الحالية نقل بقية Portal Schema إلى المسار التجاري بدون Seed أو افتراضات Staging/AAS.
5. بناء **Provisioner — أداة التجهيز** الآمنة لإنشاء مستخدمي Auth للمدير، وتسجيل UUID في جداول العضوية، وتهيئة هوية المنشأة والتواصل والعقد ومراجع الأصول الخاصة بالعميل بدون أسرار في GitHub.
6. تجهيز Backend Core وPortal مستقلين ونظيفين لأول نسخة تجريبية وتطبيق سلسلة الترحيلات من الصفر.
7. التأكد أن المخطط الكامل المطلوب للمنتج — الصور، التواريخ غير المتاحة، الأسعار، المواسم، التواصل، الملاحظات/النشاط، وتشييك العامل — يُنشأ بالكامل بالمigrations دون SQL يدوي.
8. إجراء اختبار تركيب من الصفر: دخول المدير، حجز تجريبي، واتساب، عقد، بوابة العميل، تشييك العامل، التنبيهات، ثم حذف البيانات التجريبية.
9. اختبار ترقية إصدار على نسخة العميل التجريبي، بما في ذلك migration جديدة، قبل بناء آلية توزيع التحديثات لبقية العملاء.
10. بعد نجاح النسخة الأولى، بناء آلية تكرار وتحديث للإصدارات بدل النسخ اليدوي العشوائي.

## قاعدة العمل بين الأجهزة والمحادثات

GitHub هو سجل المشروع الدائم. لا تعتمد استمرارية المشروع على جهاز بعينه أو محادثة بعينها. أي قرار مهم أو تغيير فعلي يجب أن ينتهي إلى commit/PR أو تحديث موثق في هذا الملف، بحيث يمكن استكمال العمل من أي جهاز أو محادثة جديدة دون تخمين.

عند بدء محادثة جديدة، لا يُفترض أن المستخدم يتذكر تفاصيل الخطة. يجب استعادة السياق من هذا الملف، ثم شرح الخطوة الحالية له بشكل مبسط قبل اقتراح توسع جديد.

## الخطوة التالية الحالية

العمل داخل `commercial-template` فقط على استكمال **Commercial Migrations — ترحيلات قاعدة البيانات التجارية**: نقل بقية Portal Schema بالترتيب (الصور، التواريخ غير المتاحة، الأسعار، المواسم، التواصل، الميزات النهائية/سجل النشاط، وتشييك العامل)، ومراجعة كل Migration لإزالة Seed أضواء الشرق وافتراضات Staging. بعد اكتمال السلسلة يأتي **Provisioner — أداة التجهيز** ثم Fresh Install — تركيب من الصفر على Backend معزول. لا كتابة على Production ولا إنشاء عميل فعلي قبل نجاح هذا المسار واختباره.

---
آخر تحديث مرجعي: 2026-08-20
