# Branding Layer Status — حالة طبقة الهوية

هذه المرحلة تخص `commercial-template` فقط.

## ما تم

- إضافة هوية العميل إلى `ADWAA_COMMERCIAL_CONFIG.brand`: الاسم، نوع المنشأة، الموقع، والوصف.
- إضافة `commercial-branding.js` لتطبيق الهوية على النصوص والعناوين والوصف وروابط واتساب والـManifest من مصدر إعداد واحد.
- عزل مفاتيح التطبيق القديمة التي تبدأ بـ `adwaa` داخل Storage Namespace خاص بكل نسخة.
- جعل Manifest الثابت محايدًا، مع Manifest ديناميكي للنسخة التجارية.
- عزل Cache الخاص بـService Worker حسب مسار النشر وعدم حذف Cache نسخة أخرى.
- إزالة Backend أضواء الشرق الثابت من `worker-check-public.js` وربط تشييك العامل بBackend بوابة العميل الصريح.
- تحميل إعداد النسخة قبل تشييك العامل.
- إضافة اختبارات تمنع رجوع المراجع القديمة لهذه المسارات.

## ما لم يتم في هذه المرحلة

- لا تغيير على `main` أو إنتاج أضواء الشرق.
- لا كتابة على Supabase ولا تغيير Schema/Auth/Data.
- لا تطبيق Migrations تجارية على أي مشروع.
- لا إنشاء نسخة عميل فعلية بعد.

## البوابة التالية

الخطوة التالية هي **Commercial Migrations — ترحيلات قاعدة البيانات التجارية**: فصل ترحيلات البيع عن ترحيلات Staging القديمة، تعميم Seed الهوية وهوية المدير، والتأكد من أن قاعدة عميل جديدة يمكن إنشاؤها من الصفر بدون SQL يدوي وبدون بيانات أضواء الشرق.
