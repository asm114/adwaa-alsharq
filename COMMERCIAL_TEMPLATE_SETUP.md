# Commercial Template Setup — إعداد القالب التجاري

هذا الملف يخص فرع `commercial-template` فقط. الهدف أن تكون أي نسخة عميل **معزولة افتراضيًا** وألا تتصل بأي مشروع Supabase تابع لأضواء الشرق عند النسخ.

## الحالة الحالية

طبقة الإعداد موجودة في `supabase-config.staging.js` لأسباب توافقية مع التحميل الحالي. رغم الاسم القديم، الملف أصبح نقطة إعداد النسخة التجارية.

القالب **Fail-closed — يفشل مغلقًا**: إذا بقيت أي قيمة تبدأ بـ `CHANGE_ME_` فلن يسمح ملف الإعداد بإنشاء اتصال الإدارة أو البوابة. بوابة العملاء وتشييك العامل يقرآن Backend العميل صراحةً من هذه الطبقة، ولا يحملان Project Ref ثابتًا لأضواء الشرق.

طبقة `commercial-branding.js` تطبق **Branding — الهوية التجارية** على الواجهة من نفس الإعداد، وتبني Manifest ديناميكيًا للنسخة. كما تعزل مفاتيح التطبيق القديمة التي تبدأ بـ `adwaa` داخل **Storage Namespace — نطاق التخزين** الخاص بالعميل. لا تستخدم هذه الطبقة `MutationObserver` حتى لا تعيد مشكلة تعليق الواجهة القديمة.

لا يُنشر هذا الفرع لعميل قبل تعبئة الإعدادات واختبار نسخة معزولة.

## القيم المطلوبة لكل عميل

- `deploymentId`: معرف ثابت وفريد للنسخة، مثل `resort-fahad`.
- `runtimeEnvironment`: عادة `production` للنسخة المسلمة للعميل، و`staging` لنسخة الاختبار.
- `basePath`: مسار النشر الكامل الذي يبدأ وينتهي `/`.
- `namespace.storage`: نطاق مفاتيح Local Storage — التخزين المحلي.
- `namespace.auth`: نطاق Auth — تسجيل الدخول.
- `namespace.cache`: نطاق Cache/Service Worker — التخزين المؤقت وعامل الخدمة.
- `brand.name`: الاسم التجاري المختصر للمنشأة.
- `brand.businessType`: نوع المنشأة الظاهر، مثل `منتجع` أو `شاليه`.
- `brand.location`: الموقع النصي المختصر.
- `brand.description`: وصف مختصر للهوية والـPWA.
- `backends.core.projectRef`: مشروع Supabase الخاص بتطبيق الإدارة.
- `backends.core.publishableKey`: المفتاح العام لذلك المشروع.
- `backends.portal.projectRef`: مشروع Supabase المنفصل الخاص ببوابة العميل.
- `backends.portal.publishableKey`: المفتاح العام لذلك المشروع.

## قواعد أمان

1. لا تستخدم Project Ref أو Publishable Key الخاص بأضواء الشرق في نسخة عميل.
2. Backend الإدارة وBackend بوابة العميل يجب أن يكونا مختلفين في التصميم الحالي.
3. لا تضع Service Role Key أو كلمات مرور أو أسرار داخل GitHub.
4. لا تعتمد على hostname لاختيار قاعدة البيانات؛ كل نسخة تحدد Backend صراحةً.
5. يجب أن يكون لكل نسخة `deploymentId` وAuth/Storage/Cache namespace خاص بها.
6. أي عميل جديد يبدأ بقواعد بيانات نظيفة وMigrations معروفة، لا بنسخة من بيانات الإنتاج.
7. Service Worker — عامل الخدمة يحذف فقط الكاش الذي يخص نفس مسار النشر؛ لا يحذف كاش نسخة عميل أخرى على نفس النطاق.
8. `manifest.json` ثابت ومحايد كـFallback، والهوية الفعلية للنسخة تُبنى من إعداد العميل وقت التشغيل.

## Branding — الهوية التجارية

مصدر الهوية الوحيد هو `ADWAA_COMMERCIAL_CONFIG.brand`. طبقة `commercial-branding.js` تستبدل هوية أضواء الشرق القديمة في النصوص المرئية والعناوين والوصف وروابط واتساب التي تولدها الواجهة، وتعيد تطبيق الهوية بعد تغييرات الواجهة المعتادة بدون مراقبة DOM مستمرة.

وجود نصوص Legacy — قديمة داخل ملف `index.html` الكبير لا يعني أنها هوية العميل: الطبقة التجارية تستبدلها وقت التشغيل. لكن أي منطق تشغيلي جديد يجب أن يستخدم الإعداد مباشرةً بدل إضافة اسم منشأة ثابت جديد.

## Isolation — العزل

- الإدارة تستخدم `ADWAA_SUPABASE_CONFIG`.
- البوابة العامة تستخدم `ADWAA_PORTAL_SUPABASE_CONFIG` مباشرةً.
- إدارة البوابة تستخدم نفس Backend البوابة مع Auth Namespace خاص بالنسخة.
- تشييك العامل يستخدم Backend البوابة نفسه ويفشل مغلقًا إذا لم توجد إعدادات عميل.
- مفاتيح Local/Session Storage القديمة التي تبدأ بـ `adwaa` تُخزن تحت `namespace.storage` الخاص بالنسخة.
- Cache — الكاش يُعزل حسب Service Worker scope، ولا يتم حذف كاش خارج نفس النطاق.

## ترتيب العمل قبل أول عميل تجريبي

1. ✅ فصل Backend الإدارة والبوابة وتشييك العامل عن مراجع أضواء الشرق الثابتة.
2. ✅ إضافة Branding — طبقة الهوية التجارية وStorage/Cache isolation الأساسية.
3. فصل Migrations التجارية عن أي Migrations تجريبية قديمة غير مناسبة للبيع، وتعميم Seed — البيانات الابتدائية للهوية والمدير.
4. تجهيز Backend نظيف للعميل التجريبي وتطبيق Migrations من الصفر.
5. اختبار: دخول المدير، حجز تجريبي، واتساب، عقد، بوابة العميل، تشييك العامل، التنبيهات، ثم حذف البيانات التجريبية.
6. اختبار ترقية Migration لاحقة قبل اعتبار مسار التحديث جاهزًا للبيع.
