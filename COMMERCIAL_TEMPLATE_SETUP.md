# Commercial Template Setup — إعداد القالب التجاري

هذا الملف يخص فرع `commercial-template` فقط. الهدف أن تكون أي نسخة عميل **معزولة افتراضيًا** وألا تتصل بأي مشروع Supabase تابع لأضواء الشرق عند النسخ.

## الحالة الحالية

طبقة الإعداد موجودة في `supabase-config.staging.js` لأسباب توافقية مع التحميل الحالي. رغم الاسم القديم، الملف أصبح نقطة إعداد النسخة التجارية.

القالب **Fail-closed — يفشل مغلقًا**: إذا بقيت أي قيمة تبدأ بـ `CHANGE_ME_` فلن يسمح ملف الإعداد بإنشاء اتصال الإدارة. كما أن جسر بوابة العميل يمنع أول اتصال إذا لم توجد إعدادات بوابة صحيحة.

لا يُنشر هذا الفرع لعميل قبل تعبئة الإعدادات واختبار نسخة معزولة.

## القيم المطلوبة لكل عميل

- `deploymentId`: معرف ثابت وفريد للنسخة، مثل `resort-fahad`.
- `runtimeEnvironment`: عادة `production` للنسخة المسلمة للعميل، و`staging` لنسخة الاختبار.
- `basePath`: مسار النشر الكامل الذي يبدأ وينتهي `/`.
- `namespace.storage`: نطاق مفاتيح Local Storage — التخزين المحلي.
- `namespace.auth`: نطاق Auth — تسجيل الدخول.
- `namespace.cache`: نطاق Cache/Service Worker — التخزين المؤقت وعامل الخدمة.
- `brand.name`: الاسم التجاري الظاهر للمنشأة.
- `brand.location`: الموقع النصي المختصر.
- `backends.core.projectRef`: مشروع Supabase الخاص بتطبيق الإدارة.
- `backends.core.publishableKey`: المفتاح العام لذلك المشروع.
- `backends.portal.projectRef`: مشروع Supabase المنفصل الخاص ببوابة العميل.
- `backends.portal.publishableKey`: المفتاح العام لذلك المشروع.

## قواعد أمان

1. لا تستخدم Project Ref أو Publishable Key الخاص بأضواء الشرق في نسخة عميل.
2. Backend الإدارة وBackend بوابة العميل يجب أن يكونا مختلفين في التصميم الحالي.
3. لا تضع Service Role Key أو كلمات مرور أو أسرار داخل GitHub.
4. لا تعتمد على hostname لاختيار قاعدة البيانات؛ كل نسخة تحدد Backend صراحةً.
5. يجب أن يكون لكل نسخة `deploymentId` وAuth/Storage/Cache namespace خاص بها.
6. أي عميل جديد يبدأ بقواعد بيانات نظيفة وMigrations معروفة، لا بنسخة من بيانات الإنتاج.

## ملاحظة انتقالية مهمة

`resort/portal.js` ما زال يحمل بعض القيم القديمة داخل المصدر. في هذه المرحلة يمنع `resort/supabase-runtime-bridge.js` استخدامها فعليًا، ويفرض Backend البوابة الموجود في إعداد العميل على أول `createClient`.

هذا الجسر **Temporary Compatibility Bridge — جسر توافق مؤقت** وليس الشكل النهائي للمنتج. الخطوة التالية هي إزالة المراجع والهوية القديمة مباشرةً من `resort/portal.js` وHTML وManifest وService Worker ثم حذف الحاجة لهذا الاعتراض المؤقت.

## ترتيب العمل قبل أول عميل تجريبي

1. إزالة كل المراجع والهوية الخاصة بأضواء الشرق من ملفات القالب.
2. تعميم Branding — الهوية التجارية والنصوص.
3. تعميم Storage/Auth/Cache namespaces في كل الملفات ذات الصلة.
4. فصل Migrations التجارية عن أي Migrations تجريبية قديمة غير مناسبة للبيع.
5. تجهيز Backend نظيف للعميل التجريبي وتطبيق Migrations من الصفر.
6. اختبار: دخول المدير، حجز تجريبي، واتساب، عقد، بوابة العميل، تشييك العامل، التنبيهات، ثم حذف البيانات التجريبية.
7. اختبار ترقية Migration لاحقة قبل اعتبار مسار التحديث جاهزًا للبيع.
